import React from 'react'
import ReactDOM from 'react-dom/client';

function App() {
  return (
     const countReducer = (state = 0, action) => {
          switch (action.type)
          {
              case 'INCR':
                  return state + 1;
              case 'DECR':
                  return 
          }
     }
        
  )
}
export default App;